声名变量
declare @变量名 数据类型

/////////////////////////////////////////////

变量赋值
select | set  @变量名=值
*set只能用常量赋值

////////////////////////////////////////////

连接字符串用“+”

/////////////////////////////////////////////

case语句
case 表达式(无表达式)
	when  常量(条件)  then  返回值
	· · · · · · 
 	[else  返回值]
end

////////////////////////////////////////

while  条件
begin
	SQL语句
	（break）
	（contiune）
end

///////////////////////////////////////

无条件退出，不执行之后的语句
	return  表达式(无表达式)
//////////////////////////////////////
等待一定时间后执行语句
	waitfor delay  时间
		语句
	